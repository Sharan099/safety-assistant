.env
.env.local
frontend/.env.local

# FreeLLMAPI declarative seed (generated; contains provider keys)
data/freellmapi/runtime.config.json

__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/
.venv/
venv/

*.db
*.db-journal

frontend/node_modules/
frontend/.next/
frontend/tsconfig.tsbuildinfo

# Local Cursor state / skills — keep project rules trackable.
.cursor/*
!.cursor/rules/
!.cursor/rules/**

# Eval reports folders (results.json is kept as the single RAGAS artifact)
reports/
/results/
output/

# Local HF weights (baked into Docker image at build time) and the auth token
# that huggingface_hub writes beside them.
docker/hf_cache/

# Local vector store + Docling exports
data/qdrant/
data/docling/
data/vlm_figure_pass/
data/ocr_compare/
data/llm_cache/
data/logs/
data/traces/
data/answer_cache/
data/raw/
data/jobs.sqlite3
data/conversations.sqlite3
data/response_cache.sqlite3
data/cache_version.json
groundedness_violations.jsonl
numeric_hallucination.jsonl
# Timestamped eval run outputs; keep only the summary index + status ledger tracked
eval/results/*
!eval/results/results_index.json
!eval/results/RUNS_STATUS.md

# Archived orphan evaluation package — stale run artifact (do not re-track)
archive/evaluation/results.json
uv.lock
