# Deprecated evaluation fixtures (archived)

These files belong to the orphan ``python -m evaluation.run`` HTTP/API RAGAS
stack — **not** the CI / ``eval.run_full`` gate.

| File | Former role |
|------|-------------|
| `cases.json` | 10 fixed API eval cases (`evaluation/run.py`) |

Canonical goldens live under ``eval/golden_set.jsonl``.
