# Backend runtime dependencies used by Docker / Railway.
#
# Keep evaluation/test-only packages out of the image. They are useful locally
# (see backend/requirements.txt), but they make Docker Desktop export very large
# layers and can trigger BuildKit RPC EOF failures on Windows.
#
# FastAPI is capped below 0.137 because prometheus-fastapi-instrumentator 8.0.0
# crashes on _IncludedRouter objects (only used when ENABLE_PROMETHEUS_METRICS=true).
fastapi>=0.110.0,<0.137
uvicorn[standard]>=0.27.0
pydantic>=2.6.0
pydantic-settings>=2.2.0
python-dotenv>=1.0.0
loguru>=0.7.0
groq>=0.4.0
sentence-transformers>=2.3.0
einops>=0.8.0
rank-bm25>=0.2.2
numpy>=1.26.0
torch>=2.1.0
transformers>=4.38.0
langgraph>=0.2.0
langchain-core>=0.3.0
langsmith>=0.1.0
prometheus-client>=0.20.0
prometheus-fastapi-instrumentator>=7.0.0,<9.0.0
httpx>=0.27.0
anthropic>=0.39.0
redis>=5.0.0
PyYAML>=6.0.0
