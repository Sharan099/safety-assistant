# Architecture Decision Records

One file per decision: `NNNN-short-title.md`, sequential, never renumbered or
deleted (supersede with a new ADR that links back if a decision changes).

Each ADR: Status, Date, Context, Decision, Consequences. Keep them short —
link to `PRD.md` / `TRD.md` sections instead of repeating them.
