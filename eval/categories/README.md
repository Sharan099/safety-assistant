# Category shards of eval/golden_set.jsonl (one file per category).
# Regenerated by scripts/consolidate_golden_set.py — edit the consolidator
# or golden_set.jsonl, then re-split. Prefer editing golden_set.jsonl as source of truth.
#
# RAGAS categories should prefer `ground_truth_reference_chunks` (verbatim regulation
# excerpts) so context_recall has a real ground_truth. See eval/RAGAS_NAN_NOTES.md.
