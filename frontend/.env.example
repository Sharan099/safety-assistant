# Vercel frontend — API is proxied same-origin (see vercel.json)
# Do NOT point the browser directly at HF unless testing cross-origin.
NEXT_PUBLIC_API_URL=/api/v1

# Backend target for Vercel/Next rewrites (server-side proxy only)
NEXT_PUBLIC_HF_BACKEND_URL=https://sharan099-passive-safety-assistant.hf.space
