# packages/agent

Single stateful LangGraph investigation agent + deterministic tool layer
(`load_run`, `run_quality_gate`, ... `request_engineer_review`). See `TRD.md` §3, §23-24.

Not a multi-agent swarm. Built only after packages/analysis and packages/retrieval work
standalone (Phase 15).
