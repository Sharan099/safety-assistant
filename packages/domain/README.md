# packages/domain

Core domain model: Project, Vehicle, ModelVersion, Component, ComponentRevision,
SimulationRun, Artifact, SignalDefinition, Signal. See `BACKEND_SCHEMA.md` §2, §6-17.

SQLAlchemy models + Alembic migrations live here. Built in Phase 6.
