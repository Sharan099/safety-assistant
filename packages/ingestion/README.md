# packages/ingestion

Knowledge-source ingestion pipeline: register -> SHA-256 -> Docling/PyMuPDF extraction ->
quality gate -> sections/tables/figures/equations -> OKF markdown -> chunks -> embeddings.
See `TRD.md` §13-18 and `ENVIRONMENT_SETUP.md` §13.
